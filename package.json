{
  "name": "roseiies",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm -C apps/tenant dev & pnpm -C apps/studio dev",
    "dev:tenant": "pnpm -C apps/tenant dev",
    "dev:studio": "pnpm -C apps/studio dev",
    "build": "pnpm -C apps/tenant build && pnpm -C apps/studio build",
    "lint": "pnpm -C apps/tenant lint && pnpm -C apps/studio lint"
  },
  "devDependencies": {
    "@types/node": "^20",
    "turbo": "^2.7.4",
    "typescript": "^5.9.3"
  }
}